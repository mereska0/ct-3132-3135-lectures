```
struct A
{
	itn a, b;
	char c;
}
typedef struct B
{
	int b;
}B_type;
struct C
{
	int a;
	char b;
	short c;
	long long d;
	int e;
}

int main()
{
	struct A var;
	var.a = 1;
	var.b = var.a + 2;
	
	B_type bb;
	struct A var = {.b = 3; .c = 4, .a = 2};
	sizeof(struct A); //12 bytes
	sizeof(srtuct C); // 24 bytes
	return 0;
}
```
наша структура A занимает 12 байт потому что каждое из полей выравнивается до 4 байт(максимум из полей)

`offsetof(struct, var)` -  возвращает смещение (в байтах) элемента структуры или объединения от начала этой структуры. Он возвращает константу типа [[1. Базовый синтаксис, типы данных_S#Типы данных|size_t]], вычисляемую во время компиляции, и учитывает выравнивание данных

`#pragma pack` — это директива препроцессора, управляющая выравниванием (packing) членов структуры или класса в памяти

```
struct A* a_ptr = &var;
a_ptr -> b;//pizdec polniy
(*a_ptr).b;//tozhe samoe
```
### какие то там бля указатели

```
int my_foo(struct A a){}//not good
int my_foo(struct A* a){}//gooood
```

Также в одно структуре можно юзать вторую структуру
___
### Объединение
```
union D{
	char a;
	short b;
	int c;
}
```
у юниона у каждого поля оффсет 0(они хранятся рядом)
___
### Enum
```
enum N{//по умолчанию поля инт
	const1,
	const2,
	const3
};
```
